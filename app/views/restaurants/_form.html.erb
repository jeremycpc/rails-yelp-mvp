<%= simple_form_for restaurant do |f| %>
  <%= f.input :name  %>
  <%= f.input :address %>
  <%= f.input :category, collection: Restaurant::CATEGORY,
   as: :select, include_blank: "Please select one", error: "Please select one" %>
  <%= f.submit "Add a Restaurant", class: "btn btn-primary" %>
<% end %>
